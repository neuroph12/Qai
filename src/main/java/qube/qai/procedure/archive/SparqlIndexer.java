package qube.qai.procedure.archive;

/**
 * Created by rainbird on 1/28/17.
 */
public class SparqlIndexer {

    /**
     * this is meant to be for indexing pdf-directories directly and therefore
     * allowing sparql-queries to be done on the files there. that will be allowing
     * us to treat indices as what they actually are- semantic-networks and i will be able
     * to treat them as such with least amount of work- otherwise generating the semantic-network
     * of an indexed directory of text-files, probably pdf-format.
     */
}
